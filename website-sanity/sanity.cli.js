import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  api: {
    // projectId: 'p2xa2co7',
    projectId: 'vlaut80a',
    dataset: 'production'
  },

  autoUpdates: true,
})
